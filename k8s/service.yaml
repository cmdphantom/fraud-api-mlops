apiVersion: v1
kind: Service
metadata:
  name: fraud-api
  labels:
    app: fraud-api
spec:
  type: NodePort
  selector:
    app: fraud-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
      protocol: TCP